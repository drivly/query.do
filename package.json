{
  "name": "queue.do",
  "module": "worker.js",
  "version": "1.0.0",
  "description": "Query String Parsing and Generation APIs",
  "repository": "git@github.com:drivly/queue.do.git",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev worker.js",
    "deploy": "wrangler publish worker.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "dependencies": {
    "qs": "^6.11.0"
  },
  "devDependencies": {
    "eslint": "^8.23.0",
    "jest": "^28.1.3",
    "prettier": "^2.7.1",
    "wrangler": "^2.0.27"
  }
}
